// ── Workspace / Agent Files ───────────────────────────────────────────────────
export interface MdFileInfo {
  filename: string
  path: string
  size: number
  created_time: string
  modified_time: string
}
