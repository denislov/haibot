---
phase: 01-rich-media-and-external-cli-chat-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/haibot/agents/tools/external_cli.py
  - src/haibot/agents/tools/__init__.py
  - src/haibot/agents/react_agent.py
  - src/haibot/config/config.py
  - tests/unit/agents/test_external_cli_tools.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - MSG-02
requirements_addressed:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - MSG-02
user_setup: []
must_haves:
  truths:
    - Agents can call three named coding CLIs through dedicated tools instead of ad hoc shell prompts.
    - CLI runs expose transcript or final output through the normal tool-call lifecycle so chat can replay them later.
    - Missing executable, timeout, and non-zero exit outcomes are explicit and do not bypass approval or guard semantics.
  artifacts:
    - src/haibot/agents/tools/external_cli.py
    - src/haibot/config/config.py
    - src/haibot/agents/react_agent.py
    - tests/unit/agents/test_external_cli_tools.py
  key_links:
    - New CLI tools are registered in both config defaults and the runtime tool registry.
    - Transcript-producing tool output reuses the contract defined in Plan 01.
---

<objective>
Add first-class Claude Code, Codex, and Gemini CLI tools to the HaiBot agent
runtime and wire their output into the existing tool-call/chat lifecycle.

Purpose: Replace ad hoc shell usage with bounded product-level tools and make
their transcript visible to the chat UI.
Output: A shared CLI helper, three registered built-in tools, and unit tests
for availability, timeout, and failure behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rich-media-and-external-cli-chat-integration/01-CONTEXT.md
@.planning/phases/01-rich-media-and-external-cli-chat-integration/01-RESEARCH.md
@.planning/phases/01-rich-media-and-external-cli-chat-integration/01-01-PLAN.md
@src/haibot/agents/tools/shell.py
@src/haibot/agents/tools/__init__.py
@src/haibot/agents/react_agent.py
@src/haibot/config/config.py
@src/haibot/security/tool_guard/approval.py
@src/haibot/app/approvals/service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a shared external CLI helper and three named tools</name>
  <files>src/haibot/agents/tools/external_cli.py</files>
  <read_first>src/haibot/agents/tools/shell.py, src/haibot/agents/tools/__init__.py, .planning/phases/01-rich-media-and-external-cli-chat-integration/01-CONTEXT.md, .planning/phases/01-rich-media-and-external-cli-chat-integration/01-RESEARCH.md</read_first>
  <action>Create `src/haibot/agents/tools/external_cli.py` with one shared subprocess helper and exactly three async tool functions: `run_claude_code_cli(...)`, `run_codex_cli(...)`, and `run_gemini_cli(...)`. The helper must probe executable availability with `shutil.which`, resolve cwd like the existing shell tool, enforce timeout and cancellation, and normalize result fields `provider`, `command`, `status`, `exit_code`, and `output`. Probe `claude --help`, `codex --help`, and `gemini --help` before locking the non-interactive invocation path; if a CLI cannot run non-interactively, return a clear unsupported/unavailable status instead of hanging.</action>
  <verify>rg -n \"async def run_claude_code_cli|async def run_codex_cli|async def run_gemini_cli|shutil.which\" src/haibot/agents/tools/external_cli.py</verify>
  <acceptance_criteria>
    - `src/haibot/agents/tools/external_cli.py` exists
    - `src/haibot/agents/tools/external_cli.py` contains `run_claude_code_cli`, `run_codex_cli`, and `run_gemini_cli`
    - `src/haibot/agents/tools/external_cli.py` uses `shutil.which` and explicit timeout handling
  </acceptance_criteria>
  <done>The codebase has one shared implementation path for the three external CLI tools.</done>
</task>

<task type="auto">
  <name>Task 2: Register the new tools and preserve approval semantics</name>
  <files>src/haibot/agents/tools/__init__.py, src/haibot/agents/react_agent.py, src/haibot/config/config.py</files>
  <read_first>src/haibot/agents/tools/__init__.py, src/haibot/agents/react_agent.py, src/haibot/config/config.py, src/haibot/app/routers/tools.py, src/haibot/security/tool_guard/approval.py</read_first>
  <action>Add the three CLI tools to `_default_builtin_tools()` in `src/haibot/config/config.py` with user-facing descriptions and `display_to_user: true`. Export them from `src/haibot/agents/tools/__init__.py`, then register them in the `tool_functions` map inside `src/haibot/agents/react_agent.py` so they are guarded like any other built-in tool. Do not hide the subprocess call inside unrelated code paths; the runtime must treat these as first-class built-in tools so existing toggle/approval flows still apply.</action>
  <verify>rg -n \"run_claude_code_cli|run_codex_cli|run_gemini_cli\" src/haibot/config/config.py src/haibot/agents/tools/__init__.py src/haibot/agents/react_agent.py</verify>
  <acceptance_criteria>
    - `src/haibot/config/config.py` contains built-in tool entries for the three CLI tools
    - `src/haibot/agents/tools/__init__.py` exports the three CLI tools
    - `src/haibot/agents/react_agent.py` registers the three CLI tools in `tool_functions`
  </acceptance_criteria>
  <done>The new tools can be enabled, disabled, and guarded like the rest of the built-in tool set.</done>
</task>

<task type="auto">
  <name>Task 3: Cover failure modes and transcript normalization with tests</name>
  <files>tests/unit/agents/test_external_cli_tools.py</files>
  <read_first>src/haibot/agents/tools/external_cli.py, tests/unit/providers/test_openai_provider.py, tests/unit/cli/test_cli_shutdown.py</read_first>
  <action>Create `tests/unit/agents/test_external_cli_tools.py` to cover all three CLI wrappers. Mock subprocess/probe behavior and assert the normalized result shape for: executable missing, interactive-only/unsupported mode, non-zero exit, timeout, and successful output capture. Include at least one test that verifies transcript/final output text is returned via the same `output` field the frontend tool-call block already expects.</action>
  <verify>pytest tests/unit/agents/test_external_cli_tools.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/agents/test_external_cli_tools.py` exists
    - the test file contains coverage for missing executable and timeout behavior
    - `pytest tests/unit/agents/test_external_cli_tools.py -q` exits 0
  </acceptance_criteria>
  <done>CLI tool behavior is regression-tested for success and failure states.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/agents/test_external_cli_tools.py -q`
- [ ] the three CLI tool names appear in config defaults and runtime registration
- [ ] `src/haibot/agents/tools/external_cli.py` contains one shared helper path plus 3 tool wrappers
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Claude Code, Codex, and Gemini CLI tools are first-class built-ins with
  explicit failure handling
</success_criteria>

<output>
After completion, create `.planning/phases/01-rich-media-and-external-cli-chat-integration/01-02-SUMMARY.md`
</output>
