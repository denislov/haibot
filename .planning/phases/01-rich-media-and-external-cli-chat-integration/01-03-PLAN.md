---
phase: 01-rich-media-and-external-cli-chat-integration
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - frontend/src/modules/chat/components/MediaBlock.vue
  - frontend/src/modules/chat/components/MessageBubble.vue
  - frontend/src/modules/chat/components/ToolCallBlock.vue
  - frontend/src/modules/chat/composables/useChat.ts
  - frontend/src/types/chat.ts
autonomous: true
requirements:
  - RICH-01
  - RICH-02
  - RICH-03
  - RICH-04
  - CLI-04
  - CLI-05
  - MSG-01
  - MSG-02
  - MSG-03
user_setup: []
must_haves:
  truths:
    - The existing chat bubble UI can render inline image, audio, and video blocks without breaking text/reasoning/tool-call layout.
    - Tool-call blocks visibly stream and replay CLI output inside the main chat surface.
    - The UI changes preserve the current HaiBot look and do not require a new page or separate transcript panel.
  artifacts:
    - frontend/src/modules/chat/components/MediaBlock.vue
    - frontend/src/modules/chat/components/MessageBubble.vue
    - frontend/src/modules/chat/components/ToolCallBlock.vue
    - frontend/src/modules/chat/composables/useChat.ts
  key_links:
    - `useChat.ts` emits the new media blocks that `MessageBubble.vue` renders.
    - `ToolCallBlock.vue` can show progressive CLI output produced by Plan 02 without a new route or layout.
---

<objective>
Render the new rich media blocks and CLI transcript output in the existing HaiBot
chat UI while preserving the current visual language.

Purpose: Make the new backend/runtime capabilities visible and usable in the
web console.
Output: A reusable media block component, updated assistant message rendering,
and improved tool-call output presentation for CLI transcripts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rich-media-and-external-cli-chat-integration/01-CONTEXT.md
@.planning/phases/01-rich-media-and-external-cli-chat-integration/01-RESEARCH.md
@.planning/phases/01-rich-media-and-external-cli-chat-integration/01-UI-SPEC.md
@.planning/phases/01-rich-media-and-external-cli-chat-integration/01-01-PLAN.md
@frontend/src/types/chat.ts
@frontend/src/modules/chat/composables/useChat.ts
@frontend/src/modules/chat/components/MessageBubble.vue
@frontend/src/modules/chat/components/ToolCallBlock.vue
@frontend/src/assets/styles/variables.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a reusable inline media renderer</name>
  <files>frontend/src/modules/chat/components/MediaBlock.vue</files>
  <read_first>frontend/src/modules/chat/components/MessageBubble.vue, frontend/src/assets/styles/variables.css, .planning/phases/01-rich-media-and-external-cli-chat-integration/01-UI-SPEC.md</read_first>
  <action>Create `frontend/src/modules/chat/components/MediaBlock.vue` to handle exactly three variants: image, audio, and video. Use the existing CSS variable palette (`--bg-card`, `--border`, `--primary`, `--text-*`) and the spacing contract from `01-UI-SPEC.md`. The component must render inline image preview, native HTML audio controls, and native HTML video controls, and must keep a compact assistant-message footprint instead of opening a modal by default.</action>
  <verify>test -f frontend/src/modules/chat/components/MediaBlock.vue</verify>
  <acceptance_criteria>
    - `frontend/src/modules/chat/components/MediaBlock.vue` exists
    - `frontend/src/modules/chat/components/MediaBlock.vue` contains image, audio, and video rendering branches
    - the component uses existing CSS variables instead of hard-coded unrelated theme values
  </acceptance_criteria>
  <done>The chat UI has one reusable component for all three media block types.</done>
</task>

<task type="auto">
  <name>Task 2: Render rich media blocks in assistant messages</name>
  <files>frontend/src/modules/chat/components/MessageBubble.vue, frontend/src/modules/chat/composables/useChat.ts, frontend/src/types/chat.ts</files>
  <read_first>frontend/src/modules/chat/components/MessageBubble.vue, frontend/src/modules/chat/composables/useChat.ts, frontend/src/types/chat.ts, frontend/src/modules/chat/components/MediaBlock.vue</read_first>
  <action>Update `MessageBubble.vue` so assistant blocks with `kind === 'image'`, `kind === 'audio'`, and `kind === 'video'` render through `MediaBlock.vue` in the same block list as text, reasoning, and tool-call content. Keep the existing assistant bubble shell, sender label, and action bar. Do not route these media blocks through the user attachment UI. Ensure `useChat.ts` and `frontend/src/types/chat.ts` provide the exact block payload fields that `MediaBlock.vue` expects.</action>
  <verify>rg -n \"block.kind === 'image'|block.kind === 'audio'|block.kind === 'video'|MediaBlock\" frontend/src/modules/chat/components/MessageBubble.vue</verify>
  <acceptance_criteria>
    - `frontend/src/modules/chat/components/MessageBubble.vue` imports and renders `MediaBlock`
    - `frontend/src/modules/chat/components/MessageBubble.vue` contains explicit branches for `image`, `audio`, and `video`
    - user attachment rendering logic remains separate from assistant media blocks
  </acceptance_criteria>
  <done>Assistant bubbles can display the new rich block types inline.</done>
</task>

<task type="auto">
  <name>Task 3: Make ToolCallBlock suitable for live CLI transcript output</name>
  <files>frontend/src/modules/chat/components/ToolCallBlock.vue</files>
  <read_first>frontend/src/modules/chat/components/ToolCallBlock.vue, frontend/src/modules/chat/composables/useChat.ts, .planning/phases/01-rich-media-and-external-cli-chat-integration/01-UI-SPEC.md</read_first>
  <action>Extend `ToolCallBlock.vue` so it can present long-running CLI transcript output without collapsing the content away while the run is active. Keep the existing header/icon pattern, but add explicit visual states for running vs completed vs failed using the current accent and destructive colors. The transcript/output area must remain copyable, scrollable, and readable in monospace, and it must tolerate progressive updates to `toolOutput` as more transcript arrives.</action>
  <verify>rg -n \"toolOutput|loading|Copied|running|failed\" frontend/src/modules/chat/components/ToolCallBlock.vue</verify>
  <acceptance_criteria>
    - `frontend/src/modules/chat/components/ToolCallBlock.vue` still renders `toolOutput`
    - `frontend/src/modules/chat/components/ToolCallBlock.vue` contains explicit visual treatment for running and failed/completed states
    - the output area remains copyable and scrollable for long transcript content
  </acceptance_criteria>
  <done>The existing tool-call UI can host live and replayed CLI transcript output cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && pnpm build`
- [ ] `frontend/src/modules/chat/components/MediaBlock.vue` exists and is imported by `MessageBubble.vue`
- [ ] `frontend/src/modules/chat/components/ToolCallBlock.vue` still renders copyable transcript output
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Rich media and CLI transcript output are visible in the existing chat UI
  without a layout rewrite
</success_criteria>

<output>
After completion, create `.planning/phases/01-rich-media-and-external-cli-chat-integration/01-03-SUMMARY.md`
</output>
