---
phase: 01-rich-media-and-external-cli-chat-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/haibot/app/runner/utils.py
  - frontend/src/types/chat.ts
  - frontend/src/modules/chat/composables/useChat.ts
  - tests/unit/app/test_runner_utils.py
autonomous: true
requirements:
  - MSG-01
  - MSG-02
  - MSG-03
  - RICH-04
user_setup: []
must_haves:
  truths:
    - Assistant-side image, audio, and video content survives both live SSE and history replay.
    - CLI transcript or final tool output can be attached to the existing tool-call lifecycle without inventing a second chat transport.
    - Single-agent and group-chat display pipelines share one explicit block contract for new media content.
  artifacts:
    - src/haibot/app/runner/utils.py
    - frontend/src/types/chat.ts
    - frontend/src/modules/chat/composables/useChat.ts
    - tests/unit/app/test_runner_utils.py
  key_links:
    - Runtime content objects produced in src/haibot/app/runner/utils.py are mapped explicitly in frontend/src/modules/chat/composables/useChat.ts.
    - frontend/src/types/chat.ts defines every block variant used by the chat UI.
---

<objective>
Establish the shared message/display contract for rich media blocks and
tool-output replay before backend CLI tooling and frontend rendering diverge.

Purpose: Give all later work one stable protocol for media content and CLI
tool output.
Output: Updated runtime conversion logic, typed frontend block models, SSE and
history mapping support, and regression tests for message conversion.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rich-media-and-external-cli-chat-integration/01-CONTEXT.md
@.planning/phases/01-rich-media-and-external-cli-chat-integration/01-RESEARCH.md
@.planning/phases/01-rich-media-and-external-cli-chat-integration/01-UI-SPEC.md
@src/haibot/app/runner/utils.py
@src/haibot/app/channels/base.py
@frontend/src/types/chat.ts
@frontend/src/modules/chat/composables/useChat.ts
@frontend/src/modules/chat/components/MessageBubble.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the shared media and tool-output contract</name>
  <files>src/haibot/app/runner/utils.py, frontend/src/types/chat.ts</files>
  <read_first>src/haibot/app/runner/utils.py, src/haibot/app/channels/base.py, frontend/src/types/chat.ts, .planning/phases/01-rich-media-and-external-cli-chat-integration/01-CONTEXT.md</read_first>
  <action>Keep using runtime content types `image`, `audio`, and `video` in `src/haibot/app/runner/utils.py`; do not introduce alternate backend aliases. In `frontend/src/types/chat.ts`, extend `DisplayBlock.kind` to include `image`, `audio`, and `video`, and add explicit fields for media URL/format metadata plus tool-call state fields needed for replay (`toolStatus`, `toolOutput`, and any stable identifier needed to update the correct tool block). Preserve the existing `tool_call` block pathway so CLI transcript visibility stays inside the current tool-call UX.</action>
  <verify>rg -n \"kind: 'text'.*'image'.*'audio'.*'video'|toolStatus|toolOutput\" frontend/src/types/chat.ts</verify>
  <acceptance_criteria>
    - `frontend/src/types/chat.ts` contains `kind` values for `image`, `audio`, and `video`
    - `frontend/src/types/chat.ts` contains explicit typed fields for media metadata and tool-call status/output
    - `src/haibot/app/runner/utils.py` still emits runtime `ImageContent`, `AudioContent`, and `VideoContent` objects for assistant output
  </acceptance_criteria>
  <done>Shared backend/frontend block types are explicit and grep-verifiable.</done>
</task>

<task type="auto">
  <name>Task 2: Update history and SSE mapping to stop dropping rich content</name>
  <files>frontend/src/modules/chat/composables/useChat.ts</files>
  <read_first>frontend/src/modules/chat/composables/useChat.ts, frontend/src/types/chat.ts, frontend/src/modules/chat/components/MessageBubble.vue, .planning/phases/01-rich-media-and-external-cli-chat-integration/01-RESEARCH.md</read_first>
  <action>Modify both `convertHistoryToDisplay()` and the streaming `onEvent()` path in `frontend/src/modules/chat/composables/useChat.ts` so assistant-side `content` items of type `image`, `audio`, and `video` create explicit display blocks with stable ids. Preserve current `call_id`-based tool-call block updates; tool output for CLI runs must continue to update the matching tool block instead of spawning duplicate bubbles. Reuse existing group-chat routing maps (`msgIdToAgentId`, `agentMsgBlockMap`, `agentCallBlockMap`) so the same logic works in single and group modes.</action>
  <verify>rg -n \"type === 'image'|type === 'audio'|type === 'video'|call_id\" frontend/src/modules/chat/composables/useChat.ts</verify>
  <acceptance_criteria>
    - `frontend/src/modules/chat/composables/useChat.ts` contains explicit mapping branches for assistant `image`, `audio`, and `video` content
    - Streaming event handling in `frontend/src/modules/chat/composables/useChat.ts` still updates tool blocks by `call_id`
    - No new detached transcript-only bubble path is introduced in the composable
  </acceptance_criteria>
  <done>Live SSE and history replay both map rich content into the same display contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for runtime message conversion</name>
  <files>tests/unit/app/test_runner_utils.py</files>
  <read_first>src/haibot/app/runner/utils.py, tests/unit/providers/test_openai_provider.py, tests/integrated/test_app_startup.py</read_first>
  <action>Create `tests/unit/app/test_runner_utils.py` to exercise `agentscope_msg_to_message()` with assistant messages that contain image, audio, and video blocks plus tool results carrying `call_id`, `name`, and `output`. Assert the converted runtime messages keep the expected content types, URLs/data fields, and tool output identifiers so later frontend replay work has a tested contract.</action>
  <verify>pytest tests/unit/app/test_runner_utils.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/app/test_runner_utils.py` exists
    - `tests/unit/app/test_runner_utils.py` contains assertions for image, audio, and video conversion
    - `pytest tests/unit/app/test_runner_utils.py -q` exits 0
  </acceptance_criteria>
  <done>Message conversion for the new block types is covered by a dedicated unit test.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/unit/app/test_runner_utils.py -q`
- [ ] `frontend/src/types/chat.ts` declares the new display block kinds
- [ ] `frontend/src/modules/chat/composables/useChat.ts` maps rich content for history and SSE
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Live and replayed chat data share one explicit contract for the new content
  types
</success_criteria>

<output>
After completion, create `.planning/phases/01-rich-media-and-external-cli-chat-integration/01-01-SUMMARY.md`
</output>
